# Push Mockup Generator

Generate pixel-perfect iOS push notification mockups for Tapcart prospecting.

## Fixes

- Uses Inter font (web font) instead of SF Pro Display (system font) for correct html2canvas rendering
- Tighter spacing and correct icon sizing matching iOS notification specs
